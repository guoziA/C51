
		ORG 100H
		MOV A, #0FEH
LOOP:	MOV P0, A
		LCALL DELAY
		RL A
		LJMP LOOP
		
DELAY:	MOV R0, #038H
LOOP2:	MOV R1, #0FAH
LOOP1:	NOP
		NOP
		NOP
		NOP
		NOP
		DJNZ R1, LOOP1
		DJNZ R0, LOOP2
		RET
		
		END